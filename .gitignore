__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Portable MongoDB (no local install/admin rights available) + its data dir.
/.local/

# Generated synthetic dataset (phase 4) — regenerate with `python -m data.generate`.
# Not committed: it is large, teacher-model dependent, and reproducible from seed.
/data/train.jsonl
/data/eval.jsonl
/data/generation_report.json
/data/_generated.jsonl
/data/_generated.meta.json
/data/_run.log
/data/scenarios/

# QLoRA outputs (phase 5).
/training/out/

# RAG index (phase 6) — rebuilt from rag/corpus/ with `python -m rag.ingest`.
/rag/index/

# local dev scratch
/backend/_*.log
/frontend/_*.json

# ESP32 firmware — real secrets live here
/hardware/esp32_rf_probe/config.h
